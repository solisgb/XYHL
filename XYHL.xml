<?xml version="1.0"?>
<xyhl>
<!--contiene la configuración de los proyectos para hacer gráficos temporales con líneas horizontales: 
1. db: base de datos
2. select dedonde se extraen los datos-->
	<project name="VM red piezo sequía 2018">
		<db>E:\BBDD\ipasub97.mdb</db>
		<select_master>
		SELECT IPA1.COD, IPA1.TOPONIMIA, ACUIFEROS.NOMACU, ACUIFEROS.MASUB, IPA1_UMBRAL.COD_UMBRAL, IPA1_UMBRAL.UMBRAL, IPA1_UMBRAL.FECHA1, IPA1_UMBRAL.FECHA2
		FROM (ACUIFEROS INNER JOIN IPA1 ON ACUIFEROS.COD_ACU = IPA1.ACUIFERO) INNER JOIN IPA1_UMBRAL ON IPA1.COD = IPA1_UMBRAL.COD
		WHERE IPA1_UMBRAL.COD_UMBRAL='DIA20111215'
		ORDER BY IPA1.COD;
			<umbral_columns cod="5" umbral="6" fecha1="7" fecha2="8"/>
			<parameter_select_data_column>2</parameter_select_data_column>
		</select_master>
		<select_data fecha_column="2" value_column="4">
		SELECT IPA2.COD, IPA2.FECHA, IPA2.SITUACION, [ipa1].[z]-[pnp] AS CNP
		FROM IPA1 INNER JOIN IPA2 ON IPA1.COD = IPA2.COD
		WHERE IPA2.COD=?;
		</select_data>
		<graph>
			<title>Evolución piezométrica en el sondeo {}
				<column>2</column>
			</title>
			<title>Acuífero {}
				<column>4</column>
			</title>
			<y_axis_name>CNP m.s.n.m.</y_axis_name>
		</graph>
		<dir_out></dir_out>
	</project>
</xyhl>